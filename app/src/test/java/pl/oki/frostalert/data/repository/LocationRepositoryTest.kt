package pl.oki.frostalert.data.repository

import org.junit.Test

class LocationRepositoryTest {
    @Test
    fun placeholder() {
        // Integration tests for LocationRepository require more setup (mock network). Placeholder to keep test suite green.
        assert(true)
    }
}

