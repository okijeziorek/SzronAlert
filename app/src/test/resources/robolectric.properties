# Pin Robolectric to SDK 33 — SDK 36 resources are not yet supported by
# Robolectric 4.10.x.  Without this, tests that use ApplicationProvider
# (e.g. FrostCheckWorkerTest) are silently skipped.
sdk=33
